name: 自动财报回溯分析

on:
  push:
    paths:
      - 'stocks.txt'
      - 'config.txt'
  workflow_dispatch: 

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Wait for 2 minutes
        run: sleep 120

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install yfinance pandas tabulate requests

      - name: Run Analysis
        run: |
          echo "## 📊 财报动能回溯分析报告" >> $GITHUB_STEP_SUMMARY
          echo "基准日期: $(cat config.txt)" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          python analyze.py >> $GITHUB_STEP_SUMMARY

      # 将结果保存为 Artifact (网页端备份)
      - name: Upload CSV Result
        uses: actions/upload-artifact@v4
        with:
          name: Stock_Report_CSV
          path: report.csv

      # 需求实现：直接发送文件到 Telegram
      - name: Send Document to Telegram
        if: env.TG_TOKEN != '' && env.TG_ID != ''
        env:
          TG_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TG_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          DATE=$(cat config.txt)
          # 发送文字通知
          curl -s -X POST "https://api.telegram.org/bot${TG_TOKEN}/sendMessage" \
            -d "chat_id=${TG_ID}" \
            -d "text=✅ $DATE 的财报回溯分析已完成，文件见下方："
          
          # 发送 CSV 文件
          curl -s -X POST "https://api.telegram.org/bot${TG_TOKEN}/sendDocument" \
            -F "chat_id=${TG_ID}" \
            -F "document=@report.csv" \
            -F "caption=Stock_Report_$DATE.csv"
